errors 0
UNDELIVERED (no arm emitted a recipe): ExternalSystemHasAverageResponseTimeForInterval
UNDELIVERED (no arm emitted a recipe): ExternalSystemHasErrorRateForInterval
UNDELIVERED (no arm emitted a recipe): ProviderResourceHasMonthlyCostForBillingPeriod
UNDELIVERED (no arm emitted a recipe): ServiceHasMonthlyCostForBillingPeriod
*External System has average Response Time1 for Interval if and only if
some Log Entry has that External System
and  has some Response Time2
and  is in that Interval
where Response Time1 = Mean(each Response Time2 for  each unique External System, Interval combination).

*External System has Error Rate for Interval if and only if
some Log Entry has that External System
and  has some HTTP Status where GreaterThanOrEqual(HTTP Status, 400)
and  is in that Interval
where Error Rate = Count(each Log Entry for  each unique External System, Interval combination).

*Provider Resource has monthly Cost for Billing Period if and only if
some Invoice Line Item belongs to that Provider Resource
and  has some Amount
and  has some Invoice that has that Billing Period
where Cost = Sum(each Amount for  each unique Provider Resource, Billing Period combination).

*Service has monthly Cost1 for Billing Period if and only if
some Provider Resource supports that Service
and  has some monthly Cost2 for that Billing Period
where Cost1 = Sum(each Cost2 for  each unique Service, Billing Period combination).

