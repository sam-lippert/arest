# A prefix test on a bound value: the chain emits a starts step, so the closure can filter on it

External System(.name) is an entity type.
URI is a value type.

External System has URI.
External System is secure. *

* External System is secure iff External System has some URI and that URI starts with 'https://'.
