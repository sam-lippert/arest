errors 0
UNDELIVERED (a sum over a column the reading never typed (Label Code)): BinHasCodeTotal
UNDELIVERED (an aggregate this grammar has no form for (mean)): BinHasMeanCount
UNDELIVERED (a sum over a column typed decimal (Gram Weight): a number stays an atom in the store and + throws on it): BinHasTotalWeight
*Bin has Code Total if and only if
that Bin holds some Item that has some Label Code
where Code Total = Sum(each Label Code for  that Bin).

*Bin has Mean Count if and only if
that Bin holds some Item that has some Unit Count
where Mean Count = Mean(each Unit Count for  that Bin).

*Bin has Total Count if and only if
that Bin holds some Item that has some Unit Count
where Total Count = Sum(each Unit Count for  that Bin).

*Bin has Total Weight if and only if
that Bin holds some Item that has some Gram Weight
where Total Weight = Sum(each Gram Weight for  that Bin).

*Bin has Urgent Total if and only if
that Bin holds some Item that has some Unit Count
and that Item has some Priority where GreaterThanOrEqual(Priority, 2)
where Urgent Total = Sum(each Unit Count for  that Bin).

*Depot has Yearly Total for Season if and only if
that Depot stocks some Bin that holds some Item that has some Unit Count
and that Item ships in that Season
where Yearly Total = Sum(each Unit Count for  each unique Depot, Season combination).

