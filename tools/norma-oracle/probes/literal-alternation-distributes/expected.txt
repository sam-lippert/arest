errors 0
UNBUILT (head resolves, no arm matched the body) [legs resolving 3/6; chain arm: clause names no fact type: Date is more than 30 days in the past]: * Log Entry must be purged iff Log Entry concerns EEA Customer and Log Entry has Endpoint and that Endpoint is Meter Endpoint and that Meter Endpoint has Identifier Sensitivity 'vehicle-identifier' and Log Entry has Date and Date is more than 30 days in the past.
UNBUILT (head resolves, no arm matched the body) [legs resolving 3/6; chain arm: clause names no fact type: Date is more than 30 days in the past]: * Log Entry must be purged iff Log Entry concerns EEA Customer and Log Entry has Endpoint and that Endpoint is Meter Endpoint and that Meter Endpoint has Identifier Sensitivity 'plate-identifier' and Log Entry has Date and Date is more than 30 days in the past.
UNDELIVERED (no arm emitted a recipe): LogEntryMustBePurged
*Log Entry is sensitive if and only if
that Log Entry has some Endpoint1
and some Meter Endpoint1 has some Identifier Sensitivity1 where Equals(Identifier Sensitivity1, vehicle-identifier)
where Equals(Endpoint1, Meter Endpoint1)
or that Log Entry has some Endpoint2
and some Meter Endpoint2 has some Identifier Sensitivity2 where Equals(Identifier Sensitivity2, plate-identifier)
where Equals(Endpoint2, Meter Endpoint2).

