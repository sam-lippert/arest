errors 0
UNDELIVERED (a prefix test against something not a literal): SourceDeclarationTargetsSourceService
UNDELIVERED (arithmetic this grammar has no primitive for (concat)): SourceRequestResolvesToTargetURL
UNDELIVERED (a comparison (contains)): StyleCandidateMatchesListing
*Source Declaration targets Source Service if and only if
that Source Declaration has some Base Path
and that Source Service has some URL
where StartsWith(Base Path, URL).

*Source Request resolves to target URL if and only if
that Source Request is for some Source Declaration that has some Base Path
and that Source Request is for some Resource Declaration that has some Resource Path
where URL = Concat(Base Path, Resource Path).

*Style Candidate matches Listing if and only if
that Style Candidate has some candidate Trim1
and that Listing has some listing Trim2
where Contains(Trim1, Trim2).

