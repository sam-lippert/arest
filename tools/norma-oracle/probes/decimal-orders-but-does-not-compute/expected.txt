errors 0
UNDELIVERED (an arithmetic operand typed decimal (Tax Rate): the mu multiplies in binary floating point and truncates division): QuoteHasTotalTaxRate
*Quote has total Tax Rate1 if and only if
that Quote has some submission Tax Rate2
and  has some response Tax Rate3
where Tax Rate1 = Add(Tax Rate2, Tax Rate3).

*Quote is over cap if and only if
that Quote has some Cap Rate
and  has some Tax Rate where GreaterThan(Tax Rate, 6.5).

*Quote is over six if and only if
that Quote has some Cap Rate
and  has some Tax Rate where GreaterThan(Tax Rate, 6).

*Quote is steep if and only if
that Quote has some Tax Rate
and  has some Cap Rate
where GreaterThan(Tax Rate, Cap Rate).

