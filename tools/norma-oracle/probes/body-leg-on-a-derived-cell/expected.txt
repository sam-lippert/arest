errors 0
*Account is currently in State if and only if
some Machine is for that Account
and  is currently in that State.

+Customer is cleared if
that Customer has some Client that is some Party that is settled.

+Customer is settled up if
that Customer has some Holder that is some Account that is currently in some State where Equals(State, clear).

+Customer is waiting if
for some Ticket, that Customer has that Ticket
and that Ticket is open.

*Party is settled if and only if
that Party has some Balance where Equals(Balance, zero).

*Ticket is open if and only if
that Ticket has some Status where Equals(Status, open).

