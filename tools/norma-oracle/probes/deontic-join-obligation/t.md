# A deontic obligation whose body JOINS two declared fact types. The single-clause
# deontic path resolved one reading and this fell through to a model note, which
# nothing reads; De Morgan turns it into the prohibited population the store carries.

Widget(.id) is an entity type.
Crate(.id) is an entity type.
Stamp Code is a value type.

Widget is packed in Crate.
Widget has Stamp Code.

It is obligatory that each Widget packed in some Crate has some Stamp Code.
